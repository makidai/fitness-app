import React from "react";
import NotFound from "../components/Home/NotFound";

const NotFoundPage = () => {
    return (
        <>
            <NotFound />
        </>
    );
};

export default NotFoundPage;
